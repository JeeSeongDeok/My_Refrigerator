<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myrefrigerator">
    <!-- Retrofit2과 Okhttp3를 사용하기 위해서 Internet Permission 추가 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRefrigerator"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.AllShelfLife">

        </activity>
        <activity android:name=".views.MainActivity">

        </activity>
        <activity android:name=".views.LoginActivity">

        </activity>
        <activity android:name=".views.SignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.SignupActivity"></activity>

    </application>

</manifest>